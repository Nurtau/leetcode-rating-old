import React from "react";
import ScoreList from "./ScoreList";

const App = () => {
	return (
		<div className="ui inverted segment"  style={{padding: "30px", minHeight: "100vh"}}>
			<ScoreList />
		</div>
	);
};

export default App;
